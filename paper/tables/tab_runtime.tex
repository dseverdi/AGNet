% numbers from results/classical_timing_lazy.json (exact greedy: precompute = vis_ms_mean,
% selection = greedy_ms_mean - vis_ms_mean; timed via agp_greedy_fast.greedy_guard_selection_lazy,
% a lazy/accelerated-greedy variant of greedy_agp.greedy_guard_selection_fast -- verified to
% produce identical guard counts and final coverage at every n in this grid, see
% agp_greedy_fast.py's module docstring) and results/discvis_greedy_timing.json (disc-vis
% greedy: precompute = disc_vis_s_mean, selection = selection_s_mean), and
% results/probe_timing.json (learned: encode+probe, a single forward pass with no
% precompute step -- precompute column is dashed by construction, selection column is that
% forward pass, device = learned_total_ms.{cpu,cuda}). Exact greedy's selection is in
% seconds (it is over an order of magnitude above the other columns); everything else is
% milliseconds. Real polygons, one per bucket for exact greedy (n exactly
% 200/500/1000/2000); disc-vis greedy at n approx 193, 480, 900, 2000.
\begin{tabular}{rrrrrrrr}
  \toprule
   & \multicolumn{2}{c}{Exact greedy} & \multicolumn{2}{c}{Disc-vis greedy} & \multicolumn{3}{c}{Geo-free learned} \\
  \cmidrule(lr){2-3}\cmidrule(lr){4-5}\cmidrule(lr){6-8}
  $n$ & precompute (ms) & selection (s) & precompute (ms) & selection (ms) & precompute & CPU (ms) & GPU (ms) \\
  \midrule
  $200$  & $119$  & $1.2$  & $338$  & $1.5$  & --- & $12$  & $6$  \\
  $500$  & $286$  & $6.3$  & $932$  & $5.9$  & --- & $33$  & $13$ \\
  $1000$ & $620$  & $20.4$ & $1664$ & $19.8$ & --- & $89$  & $25$ \\
  $2000$ & $1609$ & $83.6$ & $4196$ & $77.9$ & --- & $264$ & $57$ \\
  \bottomrule
\end{tabular}
